import common from './common.json'
import header from './header.json'
import checkout from './checkout.json'

const ua = {
  translations: {
    common,
    header,
    checkout
  }
}

export default ua
